#include <iostream>

int main() {
    int x = 10;
    int y = 20;

    // Multiple if statements without else if or else
    if (x < y) {
        std::cout << "First If block: x is less than y" << std::endl;
    }

    else (x == 10) {
        std::cout << "Second If block: x is equal to 10" << std::endl;
    }

    return 0;
}
